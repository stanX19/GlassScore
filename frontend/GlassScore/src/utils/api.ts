import { Patient, Doctor, ADRReport, DDICheck, ChatMessage, ChatResponse, AttachContent, Notification } from '../types/api';
import { BASE_URL } from './constants';

const API_BASE_URL = BASE_URL;

class ApiService {

}
export const apiService = new ApiService();